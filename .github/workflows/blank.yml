# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: windows-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Install Power Platform Tools
        uses: microsoft/powerplatform-actions/actions-install@v1

      - uses: actions/checkout@v2
        with:
         lfs: true

      - name: who-am-i action
        uses: microsoft/powerplatform-actions/who-am-i@v0
        with:
         environment-url: 'https://orgdb1b1774.crm4.dynamics.com/'
         app-id: 'c37f4956-70a3-4e74-a2d2-225b579d1a21'
         client-secret: 'gsY8Q~M5S6692nUi7~.x7FV9hosnWKNI2dTebb3T'
         tenant-id: '302edaf0-65d6-449a-a796-b83cfb733f0d'
        
      - name: Pack solution
        uses: microsoft/powerplatform-actions/pack-solution@v0
        with:
         solution-folder: ${{ inputs.solution_source_folder}}/${{ inputs.solution_name }}
         solution-file: ${{ inputs.solution_outbound_folder}}/${{ inputs.solution_name }}.zip
         solution-type: Unmanaged

      - name: Import solution as unmanaged to build env
        uses: microsoft/powerplatform-actions/import-solution@v0
        with:
         environment-url: 'https://orgdb1b1774.crm4.dynamics.com/'
         app-id: 'c37f4956-70a3-4e74-a2d2-225b579d1a21'
         client-secret: 'gsY8Q~M5S6692nUi7~.x7FV9hosnWKNI2dTebb3T'
         tenant-id: '302edaf0-65d6-449a-a796-b83cfb733f0d'
         solution-file: ${{ inputs.solution_outbound_folder}}/${{ inputs.solution_name }}.zip
         force-overwrite: true
         publish-changes: true

      # - name: Export solution as managed
      #   uses: microsoft/powerplatform-actions/export-solution@v0
      #   with:
      #    environment-url: 'https://orgdb1b1774.crm4.dynamics.com/'
      #    app-id: 'c37f4956-70a3-4e74-a2d2-225b579d1a21'
      #    client-secret: 'gsY8Q~M5S6692nUi7~.x7FV9hosnWKNI2dTebb3T'
      #    tenant-id: '302edaf0-65d6-449a-a796-b83cfb733f0d'
      #    solution-name: ${{ inputs.solution_name }}
      #    managed: true
      #    solution-output-file: ${{ inputs.solution_shipping_folder}}/${{ inputs.solution_name }}_managed.zip

      # - name: Unpack Solution
      #   uses: microsoft/powerplatform-actions/unpack-solution@v1
      #   with:
      #     solution-file: 'out/aSolution1.zip'
      #     solution-folder: 'out/solutions/solution one'
      #     solution-type: 'Unmanaged'
      #     overwrite-files: true

      # - name: Publish Solution
      #   uses: microsoft/powerplatform-actions/publish-solution@v1
      #   with:
      #     environment-url: 'https://orgb7f7bf9c.crm.dynamics.com/'
      #     user-name: ${{ secrets.MYUSERNAME }}
      #     password-secret: ${{ secrets.PASSWORD }}
